// Compatibility re-export. App code can migrate independently while the reusable
// camera now belongs to the engine authoring layer.
export { OrbitCamera, type OrbitState } from '../engine/orbit.ts';
